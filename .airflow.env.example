# Template for airflow.env — copy to airflow.env and fill in FIRMS_MAP_KEY.
# DATABASE_URL uses 'db' (Docker service name), not 'localhost'.
DATABASE_URL=postgresql://postgres:postgres@db:5432/satellite_tracking
FIRMS_MAP_KEY=your_map_key_here
